---
stages:
    - name: "lint"
    - name: "test"

language: generic

python:
    - "3.6"

# --------------------------------------------------------------------------
# Linting
# --------------------------------------------------------------------------

jobs:
    include:
        - stage: "lint"
          install: 
            - pip install tox-travis
          script:
            - tox
        - stage: "pytest"
          script:
            - pytest
